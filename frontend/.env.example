# Dev: leave unset so the app calls `/api/...` and Vite proxies to Symfony (see `vite.config.ts`).
# Use when serving the built UI without a reverse proxy to the API:
# VITE_API_BASE_URL=http://127.0.0.1:8000
