# The frontend calls the API on the same origin via `/api`.
# In development, Vite already proxies `/api` to `http://127.0.0.1:8000`.
#
# Do not define `VITE_API_BASE_URL` unless the backend explicitly supports
# cross-origin requests (CORS) for the target frontend origin.
